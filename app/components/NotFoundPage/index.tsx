import { Objectable } from '~/types'

function NotFoundPage(props: Objectable) {
  const { error, settings } = props

  return <div className="">not found page nha ba con</div>
}

export default NotFoundPage
